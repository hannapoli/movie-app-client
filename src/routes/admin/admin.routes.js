const express = require('express');
const router = express.Router();

//renderizar dashboard con acceso al panel de gestión de usuarios y gestion de pelis
router.get('/', (req, res) => { })




module.exports = router;